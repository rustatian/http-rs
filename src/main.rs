use std::collections::HashMap;

fn main() {
    let mut apple_map = HashMap::from([("Red Delicious", 2), ("Kawana Apple", 5)]);
    let s: String = String::from("test");
    print!("Hello, world! {}", s);
}